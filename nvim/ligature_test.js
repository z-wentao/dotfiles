// 连字符测试文件
// 应该显示为连字符的组合：

// 箭头函数
const arrow = () => { return true; }

// 比较运算符
if (x === y) {}
if (x !== y) {}
if (x <= y) {}
if (x >= y) {}

// 逻辑运算符
const logic = a && b || c

// 其他连字符
const spread = [...array]
const destructure = {foo, bar}
// 注释应该显示为 ->
// 管道操作 |>
// 等等 ...